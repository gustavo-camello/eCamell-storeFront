// Services endpoints
export const customersEndpoint = `http://localhost:3010/api/graphql`;
export const productsEndpoin = `http://localhost:3000/api/graphql`;

export const prodEndpoint = `fill me in when we deploy`;
export const perPage = 2;
